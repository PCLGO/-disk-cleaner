<Window x:Class="DiskCleanupAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="磁盘清理助手" Width="860" Height="580" MinWidth="820" MinHeight="540"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize"
        Background="#F8F9FB" FontFamily="Microsoft YaHei UI" FontSize="13"
        Icon="Assets/DiskCleanupAssistant.ico">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="ShellBrush" Color="#F8F9FB" />
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SidebarBrush" Color="#D8E2F2" />
        <SolidColorBrush x:Key="AccentBrush" Color="#1769C2" />
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#E7F0FB" />
        <SolidColorBrush x:Key="TextBrush" Color="#202731" />
        <SolidColorBrush x:Key="MutedBrush" Color="#687383" />
        <SolidColorBrush x:Key="BorderBrush" Color="#D7DCE5" />
        <SolidColorBrush x:Key="DangerBrush" Color="#C85A4A" />
        <SolidColorBrush x:Key="HealthyBrush" Color="#3E8991" />

        <Style x:Key="ChromeButton" TargetType="Button">
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#4B5563" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Cursor" Value="Arrow" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ChromeBackground" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ChromeBackground" Property="Background" Value="#E7EAF0" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ChromeBackground" Property="Background" Value="#DCE1E9" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource ChromeButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#D9485F" /><Setter Property="Foreground" Value="White" /></Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Margin" Value="0,2" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#71809A" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="NavSurface" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="NavSurface" Property="Background" Value="#C6D5EA" /><Setter Property="Foreground" Value="#245B9B" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="NavSurface" Property="Background" Value="#B8CAE3" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButtonSelected" TargetType="Button" BasedOn="{StaticResource NavButton}">
            <Setter Property="Background" Value="#1769C2" />
            <Setter Property="Foreground" Value="White" />
        </Style>

        <Style x:Key="NavGlyph" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="Margin" Value="3" />
            <Setter Property="Background" Value="#EEF2F7" />
            <Setter Property="Foreground" Value="#293443" />
            <Setter Property="BorderBrush" Value="#CAD1DC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonSurface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonSurface" Property="Opacity" Value="0.88" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonSurface" Property="Opacity" Value="0.74" /></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="ButtonSurface" Property="Opacity" Value="0.48" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#1769C2" />
            <Setter Property="BorderBrush" Value="#1769C2" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="18,0" />
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FFF0EE" />
            <Setter Property="BorderBrush" Value="#E7B7B0" />
            <Setter Property="Foreground" Value="#A43D31" />
        </Style>

        <Style x:Key="HomeShortcutButton" TargetType="Button">
            <Setter Property="Width" Value="178" />
            <Setter Property="Height" Value="48" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="9,5" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D5DCE6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ShortcutSurface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ShortcutSurface" Property="Background" Value="#F6FAFE" />
                                <Setter TargetName="ShortcutSurface" Property="BorderBrush" Value="#9EBFDF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ShortcutSurface" Property="Background" Value="#E8F1FB" />
                                <Setter TargetName="ShortcutSurface" Property="BorderBrush" Value="#6E9FCC" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ShortcutSurface" Property="BorderBrush" Value="#1769C2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HomeShortcutGlyph" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Foreground" Value="#1769C2" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="BorderBrush" Value="#C9D0DB" />
            <Setter Property="Background" Value="White" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="BorderBrush" Value="#C9D0DB" />
            <Setter Property="Background" Value="White" />
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D7DCE5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#E7EAF0" />
            <Setter Property="VerticalGridLinesBrush" Value="#EEF1F5" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC" />
            <Setter Property="RowHeight" Value="30" />
            <Setter Property="ColumnHeaderHeight" Value="32" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EEF2F7" />
            <Setter Property="Foreground" Value="#556171" />
            <Setter Property="BorderBrush" Value="#D7DCE5" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8,0" />
        </Style>

        <Style x:Key="HiddenTabs" TargetType="TabItem"><Setter Property="Visibility" Value="Collapsed" /></Style>

        <Style x:Key="ThinProgress" TargetType="ProgressBar">
            <Setter Property="Height" Value="6" />
            <Setter Property="Background" Value="#E5E9EF" />
            <Setter Property="Foreground" Value="#3E8991" />
            <Setter Property="BorderBrush" Value="#CDD3DD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                            <Grid x:Name="PART_Track">
                                <Rectangle x:Name="PART_Indicator" HorizontalAlignment="Left" Fill="{TemplateBinding Foreground}" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScanProgress" TargetType="ProgressBar">
            <Setter Property="Height" Value="6" />
            <Setter Property="Background" Value="#E5E9EF" />
            <Setter Property="Foreground" Value="#3E8991" />
            <Setter Property="BorderBrush" Value="#CDD3DD" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Window.Resources>

    <Border BorderBrush="#BFC6D1" BorderThickness="1" Background="#F8F9FB">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border x:Name="TitleBar" Grid.Row="0" Background="#F7F8FA" BorderBrush="#D7DCE5" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                        <Image Source="Assets/DiskCleanupAssistant.ico" Width="22" Height="22" Stretch="Uniform" Margin="0,0,9,0" />
                        <TextBlock Text="磁盘清理助手" FontSize="14" FontWeight="SemiBold" Foreground="#202731" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Style="{StaticResource ChromeButton}" Content="&#xE921;" AutomationProperties.Name="最小化" Click="MinimizeWindow_Click" />
                        <Button Style="{StaticResource ChromeButton}" Content="&#xE922;" AutomationProperties.Name="最大化或还原" Click="MaximizeWindow_Click" />
                        <Button Style="{StaticResource CloseButton}" Content="&#xE8BB;" AutomationProperties.Name="关闭" Click="CloseWindow_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="66"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" BorderBrush="#C0CBDA" BorderThickness="0,0,1,0">
                    <DockPanel>
                        <Button x:Name="SettingsNavButton" DockPanel.Dock="Bottom" Style="{StaticResource NavButton}" Tag="7" Click="Nav_Click" ToolTip="设置" AutomationProperties.Name="设置" Margin="0,0,0,10">
                            <TextBlock Style="{StaticResource NavGlyph}" Text="&#xE713;" />
                        </Button>
                        <StackPanel x:Name="NavPanel" Margin="0,10,0,0">
                            <Button Style="{StaticResource NavButtonSelected}" Tag="0" Click="Nav_Click" ToolTip="首页" AutomationProperties.Name="首页"><TextBlock Style="{StaticResource NavGlyph}" Text="&#xE80F;" /></Button>
                            <Button Style="{StaticResource NavButton}" Tag="1" Click="Nav_Click" ToolTip="快速清理" AutomationProperties.Name="快速清理"><TextBlock Style="{StaticResource NavGlyph}" Text="&#xE74D;" /></Button>
                            <Button Style="{StaticResource NavButton}" Tag="2" Click="Nav_Click" ToolTip="深度扫盘" AutomationProperties.Name="深度扫盘"><TextBlock Style="{StaticResource NavGlyph}" Text="&#xE721;" /></Button>
                            <Button Style="{StaticResource NavButton}" Tag="3" Click="Nav_Click" ToolTip="大文件与疑似项" AutomationProperties.Name="大文件与疑似项"><TextBlock Style="{StaticResource NavGlyph}" Text="&#xE8B7;" /></Button>
                            <Button Style="{StaticResource NavButton}" Tag="4" Click="Nav_Click" ToolTip="重复文件" AutomationProperties.Name="重复文件"><TextBlock Style="{StaticResource NavGlyph}" Text="&#xE8C8;" /></Button>
                            <Button Style="{StaticResource NavButton}" Tag="5" Click="Nav_Click" ToolTip="软件卸载" AutomationProperties.Name="软件卸载"><TextBlock Style="{StaticResource NavGlyph}" Text="&#xE71D;" /></Button>
                            <Button Style="{StaticResource NavButton}" Tag="6" Click="Nav_Click" ToolTip="清理记录" AutomationProperties.Name="清理记录"><TextBlock Style="{StaticResource NavGlyph}" Text="&#xE81C;" /></Button>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <Grid Grid.Column="1" Background="{StaticResource ShellBrush}">
                    <Grid.RowDefinitions><RowDefinition x:Name="PageHeaderRow" Height="46"/><RowDefinition Height="*"/><RowDefinition x:Name="StatusBarRow" Height="34"/></Grid.RowDefinitions>
                    <Border x:Name="PageHeader" Grid.Row="0" BorderBrush="#E1E5EB" BorderThickness="0,0,0,1" Padding="14,0">
                        <Grid>
                            <TextBlock x:Name="PageTitle" Text="首页" FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" />
                            <Button x:Name="ElevationButton" HorizontalAlignment="Right" VerticalAlignment="Center" Height="26" Padding="8,0" Margin="0" Click="ElevationButton_Click" ToolTip="点击切换到管理员权限" AutomationProperties.Name="切换到管理员权限">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="ElevationShieldGlyph" Text="&#xE83D;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Margin="0,0,5,0" />
                                    <TextBlock x:Name="AdminBadge" Text="普通权限" Foreground="{StaticResource TextBrush}" FontSize="11" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>

                    <TabControl x:Name="PageTabs" Grid.Row="1" Background="Transparent" BorderThickness="0" Padding="0">
                        <TabControl.ItemContainerStyle><Style BasedOn="{StaticResource HiddenTabs}" TargetType="TabItem" /></TabControl.ItemContainerStyle>

                        <TabItem>
                            <Grid>
                                <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="72"/></Grid.RowDefinitions>
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="260"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0" Padding="16,14">
                                        <StackPanel>
                                            <TextBlock Text="磁盘使用情况" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12" />
                                            <ItemsControl x:Name="DriveGrid">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border x:Name="DriveCardSurface" Tag="{Binding Root}" MouseLeftButtonUp="DriveCard_Click"
                                                                Cursor="Hand" CornerRadius="4" Margin="0,0,0,8" Padding="8,7,8,10"
                                                                Background="White" BorderBrush="#E3E7ED" BorderThickness="1"
                                                                AutomationProperties.Name="{Binding Name, StringFormat=选择磁盘 {0}}">
                                                            <StackPanel>
                                                                <Grid Margin="0,0,0,6">
                                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                                    <Ellipse x:Name="SelectionDot" Width="7" Height="7" Fill="#C6CFDA" Margin="0,0,7,0" VerticalAlignment="Center" />
                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#26303C" />
                                                                        <TextBlock Text="{Binding Label}" Foreground="#7A8492" Margin="4,0,0,0" TextTrimming="CharacterEllipsis" MaxWidth="95" />
                                                                    </StackPanel>
                                                                    <TextBlock Grid.Column="2" Text="{Binding Percent}" FontFamily="Consolas" FontSize="12">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock"><Setter Property="Foreground" Value="#3E8991"/><Style.Triggers><DataTrigger Binding="{Binding IsCritical}" Value="True"><Setter Property="Foreground" Value="#C85A4A"/></DataTrigger></Style.Triggers></Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </Grid>
                                                                <ProgressBar Maximum="100" Value="{Binding UsedPercent}">
                                                                    <ProgressBar.Style>
                                                                        <Style TargetType="ProgressBar" BasedOn="{StaticResource ThinProgress}"><Setter Property="Foreground" Value="#3E8991"/><Style.Triggers><DataTrigger Binding="{Binding IsCritical}" Value="True"><Setter Property="Foreground" Value="#C85A4A"/></DataTrigger></Style.Triggers></Style>
                                                                    </ProgressBar.Style>
                                                                </ProgressBar>
                                                                <Grid Margin="0,6,0,0">
                                                                    <TextBlock Foreground="#7A8492" FontSize="11"><Run Text="已用 "/><Run Text="{Binding Used}"/></TextBlock>
                                                                    <TextBlock Foreground="#566171" FontFamily="Consolas" FontSize="11" HorizontalAlignment="Right"><Run Text="{Binding Free}"/><Run Text=" 可用"/></TextBlock>
                                                                </Grid>
                                                            </StackPanel>
                                                        </Border>
                                                        <DataTemplate.Triggers>
                                                            <Trigger SourceName="DriveCardSurface" Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="DriveCardSurface" Property="Background" Value="#F4F8FD" />
                                                                <Setter TargetName="DriveCardSurface" Property="BorderBrush" Value="#BED4EA" />
                                                            </Trigger>
                                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                                <Setter TargetName="DriveCardSurface" Property="Background" Value="#EDF5FE" />
                                                                <Setter TargetName="DriveCardSurface" Property="BorderBrush" Value="#77AEE6" />
                                                                <Setter TargetName="SelectionDot" Property="Fill" Value="#1769C2" />
                                                            </DataTrigger>
                                                        </DataTemplate.Triggers>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            <Button Click="RefreshOverview_Click" HorizontalAlignment="Left" Content="刷新磁盘容量" Margin="0,2,0,0" />
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Background="#F8F9FB" Padding="16,14">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <StackPanel>
                                                    <TextBlock Text="扫描目标" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                                                    <TextBlock Text="快速扫描将检查常见、可解释的可清理位置" FontSize="11" Foreground="{StaticResource MutedBrush}" Margin="0,3,0,0" />
                                                </StackPanel>
                                                <Border HorizontalAlignment="Right" Background="#E7F4F2" CornerRadius="3" Padding="8,3" VerticalAlignment="Center">
                                                    <TextBlock Text="本机就绪" Foreground="#2F777C" FontSize="11" FontWeight="SemiBold" />
                                                </Border>
                                            </Grid>
                                            <Border Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                                <StackPanel>
                                                    <Border Padding="10,8" BorderBrush="#E4E8EE" BorderThickness="0,0,0,1">
                                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="&#xE8B7;" FontFamily="Segoe MDL2 Assets" Foreground="#4D6788" FontSize="16" VerticalAlignment="Center"/><StackPanel Grid.Column="1"><TextBlock Text="系统临时文件" Foreground="#27313D"/><TextBlock Text="Windows 临时目录与错误报告" Foreground="#7A8492" FontSize="10"/></StackPanel><TextBlock Grid.Column="2" Text="就绪" Foreground="#3E8991" FontSize="11" VerticalAlignment="Center"/></Grid>
                                                    </Border>
                                                    <Border Padding="10,8" Background="#FBFCFD" BorderBrush="#E4E8EE" BorderThickness="0,0,0,1">
                                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="&#xE774;" FontFamily="Segoe MDL2 Assets" Foreground="#4D6788" FontSize="16" VerticalAlignment="Center"/><StackPanel Grid.Column="1"><TextBlock Text="浏览器与应用缓存" Foreground="#27313D"/><TextBlock Text="已知且可以重新生成的缓存位置" Foreground="#7A8492" FontSize="10"/></StackPanel><TextBlock Grid.Column="2" Text="就绪" Foreground="#3E8991" FontSize="11" VerticalAlignment="Center"/></Grid>
                                                    </Border>
                                                    <Border Padding="10,8" BorderBrush="#E4E8EE" BorderThickness="0,0,0,1">
                                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" Foreground="#4D6788" FontSize="16" VerticalAlignment="Center"/><StackPanel Grid.Column="1"><TextBlock Text="旧日志与诊断数据" Foreground="#27313D"/><TextBlock Text="过期日志、崩溃转储和错误报告" Foreground="#7A8492" FontSize="10"/></StackPanel><TextBlock Grid.Column="2" Text="就绪" Foreground="#3E8991" FontSize="11" VerticalAlignment="Center"/></Grid>
                                                    </Border>
                                                    <Border Padding="10,8" Background="#FBFCFD">
                                                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" Foreground="#4D6788" FontSize="16" VerticalAlignment="Center"/><StackPanel Grid.Column="1"><TextBlock Text="回收站" Foreground="#27313D"/><TextBlock Text="仅统计；清空时需要单独确认" Foreground="#7A8492" FontSize="10"/></StackPanel><TextBlock Grid.Column="2" Text="不自动选择" Foreground="#A36A27" FontSize="11" VerticalAlignment="Center"/></Grid>
                                                    </Border>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <Border Grid.Row="1" Background="#EEF1F5" BorderBrush="#D7DCE5" BorderThickness="0,1,0,0" Padding="16,8">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                            <Button Style="{StaticResource HomeShortcutButton}" Tag="2" Click="Nav_Click" AutomationProperties.Name="前往深度扫盘">
                                                <Grid>
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/></Grid.ColumnDefinitions>
                                                    <Border Width="27" Height="27" Background="#E7F0FA" CornerRadius="4"><TextBlock Style="{StaticResource HomeShortcutGlyph}" Text="&#xE721;" /></Border>
                                                    <StackPanel Grid.Column="1" Margin="7,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="深度扫盘" Foreground="#26303C" FontSize="12" FontWeight="SemiBold" />
                                                        <TextBlock Text="逐层寻找空间占用" Foreground="#798494" FontSize="10" Margin="0,1,0,0" />
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="2" Text="&#xE76C;" FontFamily="Segoe MDL2 Assets" Foreground="#8995A5" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                                </Grid>
                                            </Button>
                                            <Button Style="{StaticResource HomeShortcutButton}" Tag="3" Click="Nav_Click" AutomationProperties.Name="前往大文件与疑似项">
                                                <Grid>
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/></Grid.ColumnDefinitions>
                                                    <Border Width="27" Height="27" Background="#E7F0FA" CornerRadius="4"><TextBlock Style="{StaticResource HomeShortcutGlyph}" Text="&#xE8B7;" /></Border>
                                                    <StackPanel Grid.Column="1" Margin="7,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="大文件" Foreground="#26303C" FontSize="12" FontWeight="SemiBold" />
                                                        <TextBlock Text="查看疑似无用内容" Foreground="#798494" FontSize="10" Margin="0,1,0,0" />
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="2" Text="&#xE76C;" FontFamily="Segoe MDL2 Assets" Foreground="#8995A5" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                                </Grid>
                                            </Button>
                                            <Button Style="{StaticResource HomeShortcutButton}" Tag="4" Click="Nav_Click" AutomationProperties.Name="前往重复文件">
                                                <Grid>
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/></Grid.ColumnDefinitions>
                                                    <Border Width="27" Height="27" Background="#E7F0FA" CornerRadius="4"><TextBlock Style="{StaticResource HomeShortcutGlyph}" Text="&#xE8C8;" /></Border>
                                                    <StackPanel Grid.Column="1" Margin="7,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="重复文件" Foreground="#26303C" FontSize="12" FontWeight="SemiBold" />
                                                        <TextBlock Text="按完整哈希确认" Foreground="#798494" FontSize="10" Margin="0,1,0,0" />
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="2" Text="&#xE76C;" FontFamily="Segoe MDL2 Assets" Foreground="#8995A5" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                                </Grid>
                                            </Button>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="12,0,2,0">
                                            <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" Foreground="#3E8991" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center" />
                                            <TextBlock Text="只读检查，不会删除文件" Foreground="#687383" FontSize="11" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Grid>
                        </TabItem>

                        <TabItem>
                            <Grid Margin="12">
                                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="86"/></Grid.RowDefinitions>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="扫描盘" VerticalAlignment="Center" Margin="2,0,4,0" />
                                    <ComboBox x:Name="QuickDriveSelector" Width="118" Height="32" Margin="0,0,5,0" SelectionChanged="QuickDriveSelector_SelectionChanged" ToolTip="选择快速扫描的磁盘；非系统盘会补充浅层缓存和大文件候选" />
                                    <Button x:Name="QuickScanButton" Width="108" Height="32" Padding="6,0" Style="{StaticResource PrimaryButton}" Click="QuickScan_Click" Content="开始快速扫描" />
                                    <Button x:Name="ConservativeSelectButton" Width="108" Height="32" Padding="6,0" Click="ConservativeSelect_Click" Content="保守选择" IsEnabled="False" ToolTip="只选择高置信、可恢复且当前可处理的推荐项目" />
                                    <Button x:Name="ToggleSelectAllButton" Width="108" Height="32" Padding="6,0" Click="ToggleSelectAll_Click" Content="全部选择" IsEnabled="False" ToolTip="选择当前列表中所有可执行的项目" />
                                    <Button x:Name="CleanSelectedButton" Width="108" Height="32" Padding="6,0" Click="ExecuteCleanup_Click" Content="清理已选" IsEnabled="False" />
                                    <Button x:Name="QuickCancelScanButton" Click="CancelScan_Click" Content="取消扫描" Visibility="Collapsed" />
                                </StackPanel>
                                <Border Grid.Row="1" Background="#F4F7FB" BorderBrush="#D9E2EF" BorderThickness="1" CornerRadius="4" Margin="0,0,0,8" Padding="10,7">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="扫描发现" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                                            <TextBlock x:Name="QuickFoundValue" Text="尚未扫描" FontSize="15" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="保守建议可清理" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                                            <TextBlock x:Name="QuickSafeValue" Text="—" Foreground="{StaticResource AccentBrush}" FontSize="15" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="其中可重建缓存" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                                            <TextBlock x:Name="QuickCacheValue" Text="—" FontSize="15" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="3">
                                            <TextBlock Text="需查看后决定" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                                            <TextBlock x:Name="QuickReviewValue" Text="—" FontSize="15" FontWeight="SemiBold" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <DataGrid x:Name="CandidateGrid" Grid.Row="2" AutoGenerateColumns="False" SelectionMode="Single" SelectionChanged="CandidateGrid_SelectionChanged">
                                    <DataGrid.Columns>
                                        <DataGridCheckBoxColumn Header="选择" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="48" />
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="54" />
                                        <DataGridTextColumn Header="大小" Binding="{Binding SizeDisplay}" IsReadOnly="True" Width="82" />
                                        <DataGridTextColumn Header="分类" Binding="{Binding CategoryDisplay}" IsReadOnly="True" Width="105" />
                                        <DataGridTextColumn Header="风险" Binding="{Binding RiskDisplay}" IsReadOnly="True" Width="54" />
                                        <DataGridTextColumn Header="置信度" Binding="{Binding ConfidenceDisplay}" IsReadOnly="True" Width="64" />
                                        <DataGridTextColumn Header="究竟/疑似是什么" Binding="{Binding Identity}" IsReadOnly="True" Width="190" />
                                        <DataGridTextColumn Header="精确路径" Binding="{Binding Path}" IsReadOnly="True" Width="*" />
                                    </DataGrid.Columns>
                                </DataGrid>
                                <Border Grid.Row="3" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="10" Margin="0,8,0,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="72"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                                        <TextBlock Text="判断依据" FontWeight="SemiBold"/><TextBlock x:Name="EvidenceText" Grid.Column="1" Text="请在上方单击一个项目，查看它为什么会被列出。" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Row="1" Text="清理影响" FontWeight="SemiBold"/><TextBlock x:Name="ImpactText" Grid.Row="1" Grid.Column="1" Text="选择项目后，说明清理后会发生什么。" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Row="2" Text="建议" FontWeight="SemiBold"/><TextBlock x:Name="RecommendationText" Grid.Row="2" Grid.Column="1" Text="未确认用途的项目不会被保守选择。" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </TabItem>

                        <TabItem>
                            <Grid Margin="12">
                                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                <WrapPanel Margin="0,0,0,8">
                                    <TextBlock Text="扫描范围" VerticalAlignment="Center" Margin="3" />
                                    <ComboBox x:Name="DriveSelector" Width="190" Margin="3" />
                                    <TextBlock Text="大文件阈值" VerticalAlignment="Center" Margin="10,3,3,3" />
                                    <ComboBox x:Name="LargeThreshold" Width="96" Margin="3" SelectedIndex="1"><ComboBoxItem Tag="104857600">100 MB</ComboBoxItem><ComboBoxItem Tag="524288000">500 MB</ComboBoxItem><ComboBoxItem Tag="1073741824">1 GB</ComboBoxItem></ComboBox>
                                    <Button Style="{StaticResource PrimaryButton}" Click="DeepScan_Click" Content="开始深度扫描" />
                                    <Button Click="PauseScan_Click" Content="暂停/继续" /><Button Click="CancelScan_Click" Content="取消" />
                                </WrapPanel>
                                <DataGrid x:Name="DeepGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" MouseDoubleClick="OpenSelectedPath_Click">
                                    <DataGrid.Columns><DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="54"/><DataGridTextColumn Header="大小" Binding="{Binding SizeDisplay}" Width="88"/><DataGridTextColumn Header="分类" Binding="{Binding CategoryDisplay}" Width="108"/><DataGridTextColumn Header="置信度" Binding="{Binding ConfidenceDisplay}" Width="72"/><DataGridTextColumn Header="说明" Binding="{Binding Identity}" Width="210"/><DataGridTextColumn Header="路径（双击打开）" Binding="{Binding Path}" Width="*"/></DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </TabItem>

                        <TabItem>
                            <Grid Margin="12">
                                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                <Border Background="#FFF7E8" BorderBrush="#E9D1A6" BorderThickness="1" Padding="9,6" Margin="0,0,0,8"><TextBlock Text="这些项目只供判断，永不自动勾选。双击可在资源管理器中定位。" Foreground="#8A5A19" /></Border>
                                <DataGrid x:Name="LargeGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" MouseDoubleClick="OpenSelectedPath_Click">
                                    <DataGrid.Columns><DataGridTextColumn Header="大小" Binding="{Binding SizeDisplay}" Width="90"/><DataGridTextColumn Header="类型" Binding="{Binding Identity}" Width="190"/><DataGridTextColumn Header="修改时间" Binding="{Binding ModifiedDisplay}" Width="130"/><DataGridTextColumn Header="判断依据" Binding="{Binding Evidence}" Width="240"/><DataGridTextColumn Header="路径" Binding="{Binding Path}" Width="*"/></DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </TabItem>

                        <TabItem>
                            <Grid Margin="12">
                                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                                <WrapPanel Margin="0,0,0,8"><TextBlock Text="只检查 ≥ 50 MB 文件；会读取内容并计算 SHA-256" VerticalAlignment="Center" Margin="3"/><Button Style="{StaticResource PrimaryButton}" Click="DuplicateScan_Click" Content="开始重复文件扫描"/><Button Click="PauseScan_Click" Content="暂停/继续"/><Button Click="CancelScan_Click" Content="取消"/></WrapPanel>
                                <DataGrid x:Name="DuplicateGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="DuplicateGrid_SelectionChanged">
                                    <DataGrid.Columns><DataGridTextColumn Header="潜在空间" Binding="{Binding SizeDisplay}" Width="100"/><DataGridTextColumn Header="副本数量" Binding="{Binding Paths.Count}" Width="80"/><DataGridTextColumn Header="证据" Binding="{Binding Evidence}" Width="290"/><DataGridTextColumn Header="首个路径" Binding="{Binding Path}" Width="*"/></DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </TabItem>

                        <TabItem>
                            <Grid Margin="12">
                                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                <WrapPanel Margin="0,0,0,8"><TextBox x:Name="AppFilter" Width="220" TextChanged="AppFilter_TextChanged" ToolTip="按名称或发布者筛选"/><Button Click="RefreshApps_Click" Content="刷新软件列表"/><Button Style="{StaticResource DangerButton}" Click="UninstallSelected_Click" Content="调用官方卸载"/><Button Click="ScanResiduals_Click" Content="检查软件残留"/></WrapPanel>
                                <DataGrid x:Name="AppsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                                    <DataGrid.Columns><DataGridTextColumn Header="软件" Binding="{Binding DisplayName}" Width="220"/><DataGridTextColumn Header="版本" Binding="{Binding VersionDisplay}" Width="105"/><DataGridTextColumn Header="发布者" Binding="{Binding Publisher}" Width="170"/><DataGridTextColumn Header="登记大小" Binding="{Binding SizeDisplay}" Width="90"/><DataGridTextColumn Header="安装位置" Binding="{Binding InstallLocation}" Width="*"/></DataGrid.Columns>
                                </DataGrid>
                                <TextBlock Grid.Row="2" Text="卸载命令来自 Windows 登记信息；残留只生成候选，不会自动删除。" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,0" FontSize="11"/>
                            </Grid>
                        </TabItem>

                        <TabItem>
                            <DataGrid x:Name="HistoryGrid" Margin="12" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns><DataGridTextColumn Header="时间" Binding="{Binding TimeDisplay}" Width="135"/><DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="65"/><DataGridTextColumn Header="估算大小" Binding="{Binding SizeDisplay}" Width="88"/><DataGridTextColumn Header="结果" Binding="{Binding Message}" Width="220"/><DataGridTextColumn Header="路径" Binding="{Binding Path}" Width="*"/></DataGrid.Columns>
                            </DataGrid>
                        </TabItem>

                        <TabItem>
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel MaxWidth="650" HorizontalAlignment="Left" Margin="16">
                                    <TextBlock Text="隐私与联网" FontSize="16" FontWeight="SemiBold" />
                                    <TextBlock Text="不收集遥测，不上传扫描结果。更新检查每天最多访问一次签名版本清单。" TextWrapping="Wrap" Margin="0,6,0,12" Foreground="{StaticResource MutedBrush}" />
                                    <CheckBox x:Name="EnableUpdateChecks" Content="每天最多检查一次签名更新清单" Margin="0,0,0,10" />
                                    <Button HorizontalAlignment="Left" Click="CheckUpdate_Click" Content="立即检查更新" />
                                    <Separator Margin="0,14" />
                                    <TextBlock Text="扫描与记录" FontSize="16" FontWeight="SemiBold" />
                                    <WrapPanel Margin="0,8,0,0"><TextBlock Text="大文件阈值" VerticalAlignment="Center" Width="105"/><ComboBox x:Name="SettingsLargeThreshold" Width="120"><ComboBoxItem Tag="104857600">100 MB</ComboBoxItem><ComboBoxItem Tag="524288000">500 MB</ComboBoxItem><ComboBoxItem Tag="1073741824">1 GB</ComboBoxItem></ComboBox></WrapPanel>
                                    <WrapPanel Margin="0,6,0,0"><TextBlock Text="重复文件阈值" VerticalAlignment="Center" Width="105"/><ComboBox x:Name="DuplicateThreshold" Width="120"><ComboBoxItem Tag="52428800">50 MB</ComboBoxItem><ComboBoxItem Tag="104857600">100 MB</ComboBoxItem><ComboBoxItem Tag="524288000">500 MB</ComboBoxItem></ComboBox></WrapPanel>
                                    <WrapPanel Margin="0,6,0,10"><TextBlock Text="记录保留" VerticalAlignment="Center" Width="105"/><ComboBox x:Name="HistoryRetention" Width="120"><ComboBoxItem Tag="7">7 天</ComboBoxItem><ComboBoxItem Tag="30">30 天</ComboBoxItem><ComboBoxItem Tag="90">90 天</ComboBoxItem></ComboBox></WrapPanel>
                                    <Button HorizontalAlignment="Left" Click="SaveSettings_Click" Content="保存设置" />
                                    <Separator Margin="0,14" />
                                    <TextBlock Text="本地数据" FontSize="16" FontWeight="SemiBold" />
                                    <TextBlock Text="便携版仍会把设置、清理记录和最新扫描快照保存在当前用户 LocalAppData。" TextWrapping="Wrap" Margin="0,6,0,8" Foreground="{StaticResource MutedBrush}"/>
                                    <TextBlock x:Name="DataPathText" Foreground="#667085" TextWrapping="Wrap" Margin="0,0,0,8" />
                                    <Button Style="{StaticResource DangerButton}" HorizontalAlignment="Left" Click="ClearLocalData_Click" Content="清除本软件本地数据" />
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>
                    </TabControl>

                    <Border x:Name="StatusBar" Grid.Row="2" Background="#F1F3F6" BorderBrush="#D7DCE5" BorderThickness="0,1,0,0" Padding="12,0">
                        <Grid>
                            <TextBlock x:Name="StatusText" Text="就绪。当前未执行任何扫描或清理。" Foreground="{StaticResource MutedBrush}" FontSize="11" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,150,0" />
                            <ProgressBar x:Name="ScanProgressBar" Style="{StaticResource ScanProgress}" Width="130" Minimum="0" Maximum="100" Value="0" IsIndeterminate="True" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
