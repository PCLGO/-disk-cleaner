---
name: disk-cleaner
description: 快速盘点 Windows C 盘或 D 盘中可清理的缓存、临时文件、崩溃转储、旧日志、开发工具缓存、疑似遗留安装包与可选重复文件，先查明每项究竟是什么、疑似用途、判断依据和删除影响，再生成只读候选报告交给用户逐项判断。用户提到磁盘空间不足、C盘爆满、D盘清理、找垃圾文件、缓存、冗余文件、重复文件或释放空间时都应使用此 skill。默认只扫描和报告；用户尚未获得足够把握或没有确认具体候选项时，不执行删除。
compatibility: Windows 10/11, PowerShell, Python 3.9+
---

# Disk Cleaner

把“发现候选项”和“执行清理”分成两个独立阶段。磁盘内容往往无法仅凭路径准确判断价值，因此先快速找出高收益候选项、解释风险，再让用户选择。

## 工作流

1. 先确认用户要检查的盘符。未说明时，在 Windows 上优先检查现有的 `C:` 和 `D:`。
2. 运行只读扫描：

   ```powershell
   python "<skill-root>\scripts\scan_candidates.py" C: D: --output "<workspace>\disk-cleaner-report.json"
   ```

3. 如果用户明确关心重复文件，再增加 `--duplicates`。重复文件扫描会读取文件内容计算哈希，通常比缓存扫描慢：

   ```powershell
   python "<skill-root>\scripts\scan_candidates.py" D: --duplicates --max-seconds 90 --output "<workspace>\disk-cleaner-report.json"
   ```

4. 读取 JSON 报告，按“建议优先级”向用户展示：

   - `low`：通常可重建的缓存或临时数据，但仍需确认应用已关闭。
   - `medium`：日志、崩溃转储、开发工具缓存等，可能影响诊断或导致重新下载。
   - `high`：疑似归档、安装包、重复文件、未知用途目录；只提示，不自行判断无用。

5. 对每个准备展示的候选项先完成“身份说明”，不能只复述目录名或笼统写成缓存：

   - **究竟是什么**：能确认的软件、Windows 组件、项目或文件类型。
   - **疑似什么**：无法确认时给出最可能用途，并明确使用“疑似/可能”措辞。
   - **判断依据**：路径归属、文件扩展名、内容结构、时间、哈希或软件约定。
   - **删除影响**：会丢失什么、是否可重建、是否影响修复/卸载/登录/诊断。
   - **置信度**：`high`、`medium` 或 `low`；置信度不是风险等级。
   - **建议动作**：官方清理、进一步检查、可考虑清理，或不建议处理。

   如果现有报告不足以说明身份，继续进行只读调查，例如查看目录第一层内容、文件属性、相邻清单文件、包管理器元数据或数字签名。不要为了得到结论而打开私人文档内容，也不要把猜测包装成事实。

6. 显示候选 ID、路径、估算大小、风险、身份说明和扫描是否完整。优先展示体积最大的 10–20 项，并给出总潜在空间；不要把“潜在空间”说成一定能释放的空间。
7. 等待用户在理解身份、依据和影响后按候选 ID 或精确路径选择。用户没有把握时，提供更多只读证据而不是推动其确认。用户的“清理一下”“都删了”不等于对高风险项的充分确认；对 `medium`/`high` 项再次列出精确目标。
8. 执行前做即时检查：

   - 重新解析绝对路径，确认仍在用户指定盘符中。
   - 确认目标没有变成符号链接或挂载点。
   - 确认不在保护路径内；保护规则见 [Windows 安全边界](references/windows-safety.md)。
   - 提醒用户关闭相关浏览器、IDE、包管理器或游戏启动器。
   - 优先使用应用自身的清理命令、Windows“存储/临时文件”或回收站；只有无法使用可恢复方式时才讨论永久删除。

9. 实际清理属于破坏性操作。只有用户明确表示已理解候选身份和影响，并确认这一批精确目标后才执行；完成后报告成功、跳过、失败和实测释放空间。不要把扫描报告本身当作删除授权。

## 快速扫描策略

扫描器先检查常见缓存位置，然后在每个盘符上进行有时间和目录数量上限的广度扫描。默认限制保证能较快返回部分高价值结果，而不是为了完整性让用户长时间等待。

- 默认最多扫描 45 秒、80,000 个目录项。
- `--max-seconds` 调整总时间预算。
- `--min-size-mb` 控制报告阈值，默认 50 MB。
- `--duplicates` 只对达到阈值的大文件分组并计算 SHA-256；超时会在报告中标记不完整。
- 权限不足的路径记录为统计信息，不将其误报为空目录。
- 扫描器从不写入被扫描盘，也没有删除参数。

## 报告格式

对用户使用简洁表格：

| ID | 位置 | 估算大小 | 风险 | 究竟/疑似是什么 | 判断依据 | 删除影响 | 置信度 | 建议 |
|---|---|---:|---|---|---|---|---|---|

表格后补充：

- 扫描盘符与耗时；
- 是否因时间、数量或权限而不完整；
- 各风险等级的候选空间小计；
- 下一步请用户回复候选 ID，例如“先处理 C003、D008”。

重复文件组必须列出所有路径，并明确说明“只应保留或删除用户指定的副本”；不要自行选择保留哪一份。

## 安全原则

- 默认只读。不要在扫描过程中顺手清理扫描报告、旧日志或任何候选文件。
- 不扫描或建议直接删除系统核心、程序安装目录、用户文档库、代码仓库中的源文件、虚拟磁盘、备份集和同步盘内容。
- 名称包含 `cache`、`temp` 或 `backup` 只是线索，不是删除证据。
- 不把 `node_modules`、虚拟环境、模型权重、游戏资源或包管理器仓库归为低风险；它们最多是 `medium`/`high`，并说明重建成本。
- 安装在 Cursor、VS Code、Electron 应用等程序目录中的 `resources\app\node_modules` 是应用组件，不是普通项目依赖，不能作为清理候选。
- `ProgramData\Package Cache` 中的安装包通常用于软件修复或卸载；即使年代较久，也不建议直接删除。
- Windows Update、Delivery Optimization、浏览器、游戏平台等托管缓存，优先建议使用官方清理入口。
- 任何路径不明确、报告过期、应用正在使用目标或目标跨越保护边界时，停止并重新检查。

## 资源

- `scripts/scan_candidates.py`：C/D 盘只读候选扫描和可选重复文件检测。
- `references/windows-safety.md`：执行清理前必须读取的 Windows 保护路径与处置建议。
- `evals/evals.json`：覆盖 C/D 盘、重复文件和删除授权边界的评测提示。
